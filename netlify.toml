# Netlify, dépôt public « petit-sommeil-site » (le dossier site/ seul, extrait du dépôt privé le 09/09/2026).
# Ici la racine du dépôt EST le site : pas de base, publish = dist.
[build]
  command = "npm run build"
  publish = "dist"

# Les en-têtes HTTP sont dans public/_headers, copié dans dist/ au build.
# Notifications des formulaires : Site configuration → Notifications → Form submission, adresse contact.petitsommeil@gmail.com.
